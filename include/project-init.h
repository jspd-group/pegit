#ifndef PROJECT_INIT_H_
#define PROJECT_INIT_H_

/**
 * initialise the project whose description in the provided in the arguments
 * arguments include project name essentially.
 */
extern int initialize_empty_project(int argc, char *argv[]);

#endif
